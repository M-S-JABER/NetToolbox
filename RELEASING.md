# إصدار تحديث جديد على App Store

دليل نشر أي تعديل أو تحسين بعد أن صار التطبيق منشوراً.
التطبيق في المتجر باسم **NetToolbox Pro**، ورقمه في App Store Connect `6803236893`،
ومعرّف الحزمة `com.m-s-jaber.nettoolbox`.

القاعدة: الإصدار المنشور يبقى عند المستخدمين كما هو إلى أن توافق Apple على التحديث،
ثم يحلّ محلّه تلقائياً.

---

## 1. التعديل

- اعمل على فرع جديد، لا على `main` مباشرة.
- افتح PR وانتظر نجاح الفحوص (`اختبارات NetToolboxKit` و`بناء التطبيق (غير موقّع)`)، ثم ادمجه.
- أداة جديدة = نوع يطابق `NetworkTool` في مجلد `Features/<Tool>/` + سطر في `Core/ToolKit/ToolRegistry.swift`
  + مفاتيحها في `Resources/{ar,en}.lproj/Localizable.strings` + صفحة في `docs/tools/`.

## 2. رفع رقم الإصدار (إلزامي)

Apple ترفض إصداراً رقمه مساوٍ لرقم منشور. اختر الرقم الجديد:

| نوع التغيير | مثال |
| --- | --- |
| إصلاحات صغيرة | `2.3.2` ← `2.3.3` |
| ميزات أو أدوات جديدة | `2.3.2` ← `2.4.0` |
| تغيير كبير في التطبيق | `2.3.2` ← `3.0.0` |

عدّله في مكانين يجب أن يتطابقا:

- `project.yml` ← `MARKETING_VERSION`
- `NetToolbox.swiftpm/Package.swift` ← `displayVersion`

**رقم البناء** (build) يجب أن يزيد في كل رفع، ولا حاجة لتعديله يدوياً:
يأخذه `ios-release` تلقائياً من رقم تشغيل الـworkflow.

## 3. البناء والرفع إلى TestFlight

### الطريق الأساسي: GitHub Actions

1. في المستودع: **Actions ← ios-release ← Run workflow**.
2. اكتب في `changelog` ملخصاً قصيراً للمختبِرين، واترك `build_number` فارغاً.
3. انتظر نحو 8 إلى 10 دقائق حتى يكتمل البناء والتوقيع والرفع.

بديل: دفع وسم يبدأ بـ`v` (مثل `v2.4.0`) يُطلق الـworkflow نفسه.
الوسوم بلا `v` (مثل `2.4.0`) لا تُطلقه.

### الطريق البديل: الماك الافتراضي

يمكن تشغيل `bundle exec fastlane beta` على الماك مع `CI=true` وقيم الأسرار نفسها
(انظر `SETUP.md`)، مع تمرير `BUILD_NUMBER` أكبر من آخر بناء مرفوع.
هذا الطريق لم يُجرَّب بعد لهذا التطبيق، فالأساسي هو GitHub Actions.

## 4. التجربة قبل النشر (يُنصح بها)

- بعد دقائق من الرفع يظهر البناء في App Store Connect ← TestFlight.
- ثبّته من تطبيق **TestFlight** على الآيفون أو الآيباد وجرّب ما تغيّر.
- المستخدمون لا يرون شيئاً في هذه المرحلة.

## 5. إنشاء الإصدار الجديد في App Store Connect

من App Store Connect ← التطبيق ← **Distribution ← + (iOS App)**، واكتب رقم الإصدار نفسه.

ينتقل من الإصدار السابق تلقائياً: الوصف، والكلمات المفتاحية، ولقطات الشاشة،
والخصوصية، والسعر، والدول، والتقييم العمري.

المطلوب في كل إصدار:

- **«ما الجديد» (What's New)** بالعربية والإنجليزية.
- اختيار **البناء** الجديد في قسم Build.

وإن تغيّر شكل الشاشات كثيراً، فأعد اللقطات بـ`scripts/store-screenshots.sh`
(التفاصيل في رأس الملف وفي `APPSTORE.md` ← Screenshots).

## 6. الإرسال للمراجعة

- اضغط **Add for Review** ثم **Submit to App Review**.
- مراجعة التحديثات غالباً أسرع من الإصدار الأول (عادةً خلال يوم).
- النشر مضبوط **تلقائياً بعد الموافقة**. يحصل على التحديث تلقائياً من فعّل التحديث
  التلقائي في جهازه، والباقون يجدونه في صفحة التحديثات في App Store.

إن رُفض الإصدار: اقرأ السبب في App Store Connect ← App Review، ثم عدّل ما يلزم
وارفع بناءً جديداً (رقم الإصدار نفسه، ورقم بناء أكبر) وأعد الإرسال.

---

## خيارات مفيدة

- **مراجعة مستعجلة (Expedited Review):** لإصلاح خلل خطير في النسخة المنشورة. تُطلب من
  developer.apple.com/contact/app-store وتُقبل غالباً خلال ساعات.
- **النشر التدريجي (Phased Release):** يصل التحديث إلى نسبة صغيرة من المستخدمين ثم يتوسّع
  خلال 7 أيام، ويمكن إيقافه مؤقتاً إن ظهرت مشكلة. يُفعَّل من صفحة الإصدار قبل الإرسال.
- **النشر اليدوي:** إن أردت اختيار لحظة النشر بنفسك، فاختر «Manually release this version»
  قبل الإرسال.

## نسخة Swift Playgrounds على الآيباد

`NetToolbox.swiftpm` يبني المكتبة من **وسم منشور** لا من آخر كود في `main`.
بعد أي تعديل تريد تجربته من الآيباد:

1. شغّل **Actions ← Tag release** بالرقم الجديد.
2. حدّث `from:` في `NetToolbox.swiftpm/Package.swift` إلى ذلك الوسم.

نسخة المتجر لا تتأثر بهذا، لأن `ios-release` يبني من الكود مباشرة.

## قائمة سريعة

- [ ] التعديل مدموج في `main` والفحوص ناجحة
- [ ] `MARKETING_VERSION` و`displayVersion` مرفوعان ومتطابقان
- [ ] `ios-release` اكتمل والبناء ظاهر في TestFlight
- [ ] جُرّب البناء على جهاز حقيقي
- [ ] إصدار جديد في App Store Connect مع «ما الجديد» بلغتين والبناء مختار
- [ ] أُرسل للمراجعة
